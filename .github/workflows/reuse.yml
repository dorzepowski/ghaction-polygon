name: "Reuse me"

on:
  workflow_dispatch:
    inputs:
      os:
        description: "OS"
        default: '["ubuntu-latest", "macos-latest"]'

jobs:
  build:
    strategy:
      matrix:
        os: ${{ fromJSON(github.event.inputs.os)}}
    runs-on:  ${{ matrix.os }}
    steps:
      - name: "OS print"
        run: "uname"
