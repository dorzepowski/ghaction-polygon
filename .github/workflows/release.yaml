name: Release
permissions:
  contents: write
  packages: write

on:
  push:
    tags:
      - 'v*'

jobs:
  release_default:
    uses: bactions/workflows/.github/workflows/release-go-server.yaml@main
    with:
      version: ${{ github.ref_name }}
    secrets: inherit
