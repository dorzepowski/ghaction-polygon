name: Release
permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:


jobs:
  release_default:
    uses: ./.github/workflows/on-release.yml
  release_single:
    uses: ./.github/workflows/on-release.yml
    with:
      os: 'linux'
