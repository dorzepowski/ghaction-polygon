name: Release
permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:


jobs:
  release_default:
    uses: ./.github/workflows/reuse.yml
  release_single:
    uses: ./.github/workflows/reuse.yml
    with:
      os: 'linux'
